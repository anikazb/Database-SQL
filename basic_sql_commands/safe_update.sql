SET SQL_SAFE_UPDATES = 0;
-- where prefers to find with the primary key attributes 
-- but if you want to use other atributes in where condition thn at first off the safe mode of query with the given command
 
-- IF YOU WANT TO ON THE SAFE MODE THN SET SQL_SAFE_UPDATES = 1;